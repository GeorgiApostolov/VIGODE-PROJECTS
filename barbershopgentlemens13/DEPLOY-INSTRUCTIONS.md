# 🚀 Инструкции за качване на промените

## Какво е променено?

### Backend (`backend/server.js`)

- ✅ userId сега се запазва правилно като ObjectId в MongoDB
- ✅ Добавена конверсия от string към ObjectId при създаване на booking

### Frontend

- ✅ Автоматично попълване на данни при логнат потребител
- ✅ Оправен localStorage ключ за токена (authToken)
- ✅ Оправен useEffect за зареждане на потребителски данни
- ✅ Профилната снимка се показва автоматично при резервация
- ✅ Добавен .htaccess за правилен SPA routing (решава 404 на /profile и други страници)

---

## 📦 Файл за качване

Създаден е файл: **`dist-update.zip`**

Съдържа:

- `/dist/` - Билднат frontend (с .htaccess файл за SPA routing)
- `/backend/server.js` - Обновен backend файл

---

## 📝 Стъпки за качване на сървъра

### Вариант 1: Чрез cPanel File Manager

1. **Логни се в cPanel**
2. **Отвори File Manager**
3. **Backup на текущите файлове** (опционално, но препоръчително):
   - Свали `public_html/dist` и `public_html/backend/server.js`
4. **Качи `dist-update.zip`** в `public_html/`
5. **Разархивирай го**:
   - Right click на `dist-update.zip` → Extract
   - Ще презапише `dist/` папката и `backend/server.js`
6. **Изтрий zip файла** след разархивиране
7. **Рестартирай backend**:
   - Отвори `public_html/backend/tmp/restart.txt`
   - Добави празен ред или промени датата
   - Запази файла

### Вариант 2: Чрез FTP (FileZilla, Cyberduck и др.)

1. **Свържи се към сървъра**
2. **Навигирай до `public_html/`**
3. **Backup** (препоръчително):
   - Свали `dist/` и `backend/server.js`
4. **Качи новите файлове**:
   - Качи целия `dist/` folder → презаписва стария
   - Качи `backend/server.js` → презаписва стария
5. **Рестартирай backend**:
   - Отвори `backend/tmp/restart.txt` и добави празен ред
   - Качи файла обратно

### Вариант 3: Чрез SSH (Най-бързо)

```bash
# 1. Качи zip файла
scp dist-update.zip user@barbershopgentlemens13.com:/home/user/

# 2. SSH в сървъра
ssh user@barbershopgentlemens13.com

# 3. Разархивирай
cd /home/user/public_html/
unzip ~/dist-update.zip -o

# 4. Рестартирай backend
touch backend/tmp/restart.txt

# 5. Изтрий zip
rm ~/dist-update.zip

# 6. Излез
exit
```

---

## ✅ Проверка след качване

1. **Отвори сайта**: https://barbershopgentlemens13.com
2. **Логни се** с потребител
3. **Отиди на "Запази час"**
4. **Провери дали**:
   - ✅ Автоматично се попълват име, имейл, телефон
   - ✅ Показва се профилната снимка
5. **Направи тестова резервация**
6. **Провери в базата дали `userId` се записва**

---

## 🔧 Ако има проблеми

### Frontend не се обновява?

- Изчисти browser cache (Ctrl+Shift+R или Cmd+Shift+R)
- Провери дали файловете в `dist/assets/` са нови (виж датата)

### Backend не работи?

- Провери логовете в cPanel
- Увери се че `tmp/restart.txt` е променен
- Провери дали `server.js` е качен правилно

### userId все още не се записва?

- Провери дали в MongoDB има индекс който блокира промяната
- Може да се наложи да изтриеш стари bookings без userId

---

## 📞 Контакт

Ако имаш въпроси или проблеми, пиши!
