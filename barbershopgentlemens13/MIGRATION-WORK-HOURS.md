# 🕐 Актуализиране на работни часове

## ⚡ Бързо решение (НАПРАВИ ТОВА СЕГА):

### В MongoDB Compass:

1. **Отвори MongoDB Compass**
2. **Свържи се към базата** (MongoDB Atlas)
3. **Избери database-а** (barbershop или test)
4. **Кликни на Collections → barbers**
5. **Долу отвори Mongosh таб**
6. **Copy-paste** това:

```javascript
db.barbers.updateOne(
  { name: /иван/i },
  { $set: { 
    workHours: { 
      start: 8, 
      end: 20, 
      wednesdayStart: 12,
      lunchBreak: true 
    }
  }}
)
```

7. **Натисни Enter**
8. Трябва да видиш: `{ acknowledged: true, modifiedCount: 1 }`

---

## ✅ Проверка дали работи:

1. **Refresh** сайта
2. **Отвори** страница за запазване на час
3. **Избери** Иван Кръстев
4. **Избери** сряда (Wednesday)
5. **Провери**: Трябва да виждаш само часове от **12:00** нататък

Ако виждаш 08:00, 09:00, 10:00, 11:00 → **не работи**
Ако първият час е 12:00 → **РАБОТИ!** ✅

---

## 📝 За всички останали барбъри (опционално):

```javascript
db.barbers.updateMany(
  { workHours: { $exists: false } },
  { $set: { 
    workHours: { 
      start: 8, 
      end: 20, 
      lunchBreak: true 
    }
  }}
)
```

---

## 🔧 Промяна на часове в бъдеще:

Пример - Теодор сега работи 10:00-18:00:

```javascript
db.barbers.updateOne(
  { name: "Теодор" },
  { $set: { 
    workHours: { 
      start: 10, 
      end: 18, 
      lunchBreak: false 
    }
  }}
)
```

Refresh сайта → промените са веднага! 🚀
